# Sovereign Protocol

- `sovereign_version`: `1.0.0`
- `protocol_version`: `1.0.0`

Capability negotiation returns honest `AVAILABLE` / `UNAVAILABLE` / `EXPERIMENTAL` per feature.
Phase B enables repository-backed: X-Ray, explain, trace, expected/effective/compare/drift.

Sovereign interfaces must not claim support for capabilities marked `UNAVAILABLE`.
