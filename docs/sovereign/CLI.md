# Sovereign CLI

Top-level aliases share implementations with `whitepact sovereign ...` (no duplicate logic).

Implemented V1 commands:

- `whitepact doctor`, `whitepact ci`, `whitepact xray`, `whitepact trace`, `whitepact explain`
- `whitepact gauntlet`, `whitepact sandbox`, `whitepact replay` (UNAVAILABLE), `whitepact prove` (UNAVAILABLE)
- `whitepact init`, `whitepact connect`, `whitepact context list|current|use`
- `whitepact sovereign status`
- `whitepact sovereign version`
- `whitepact sovereign doctor`
- `whitepact sovereign ci`
- `whitepact sovereign manifest validate`
- `whitepact sovereign simulate blast-radius`

Use `--json` for machine output. No bypass flags.
