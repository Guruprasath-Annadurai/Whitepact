# CI / supply chain (Phase 2)

Repository evidence: `.github/workflows/ci.yml`, `security-scan.yml`, `dependency-review.yml`, `pip-audit` in CI. Org branch protections are **owner evidence** unless exported from GitHub API.
